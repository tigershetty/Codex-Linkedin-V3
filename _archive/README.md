# Archive

Retired projects. Not used by the active content engine. Reversible via git.

| Folder | What | Status |
|---|---|---|
| `web-app/` | The Vite/React + Python web app (`infographic-engine-web`) + its `vercel.json` | Retired — no longer maintained. Reusable UI salvaged to `ui-kit/`. |
| `eco-mobility/` | Prior project (webp assets) | Reference only. |

If Vercel or Netlify are still connected to this repo, disconnect those projects in their
dashboards — there is no web app left to build.
