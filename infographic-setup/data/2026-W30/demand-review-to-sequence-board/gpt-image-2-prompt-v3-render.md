# GPT Image 2 Prompt v3 — Depth + Differentiation

**Purpose:** Correct v2 based on Tiger feedback: add real module depth, differentiate the sequence options, blend v1 richness with v2 brand discipline, and make logo zones feel embedded rather than pasted.

## Prompt

Use the first visible render as a reference for richness/detail/depth and the second visible render as a reference for brand palette, title treatment, and clean white/pale-blue field. Create a fresh v3 render, not a copy.

Create a world-class Shetty's Desk infographic titled `DEMAND REVIEW TO SEQUENCE BOARD`. It must combine the richness and exhaustiveness of v1 with the cleaner brand discipline of v2.

## Must Improve From v2

- `DEMAND MISS` cannot be generic. Add visible micro-details under it: `ACTUAL > FCST`, `PULL-IN`, `LATE ORDER`.
- `ROOT CAUSE` cannot be blank lines only. Add named causes: `PROMO LIFT`, `CUSTOMER PULLBACK`, `ORDER SHIFT`, `BASELINE HIGH`.
- `FAMILY IMPACT` cannot show identical tokens. Show distinct families: `FAMILY A`, `FAMILY B`, `FAMILY C`, `SERVICE RISK`, each with different icon/shape/status cue.
- `CHANGEOVER RISK` cannot show repeated identical chips. Show distinct transitions: `A TO B LOW`, `B TO C WATCH`, `C TO A HIGH` with green/amber/coral status logic.
- `SEQUENCE A`, `SEQUENCE B`, and `SEQUENCE C` cannot be identical. They must show different block order, connector path, and decision logic:
  - `SEQUENCE A` with tag `SERVICE-FIRST` and more direct service flow.
  - `SEQUENCE B` with tag `LOW CHANGEOVER` and grouped product families to reduce switches.
  - `SEQUENCE C` with tag `MATERIAL-SAFE` and a material-availability constraint cue.
- Logos must not look pasted. Reserve real embedded plates/chips: one integrated Claude chip near the center bridge with a blank inset icon slot; one integrated footer logo plate, centered and built into the visual surface.

## Brand Style

Keep the current Shetty's Desk flagship style: luminous white / very pale blue operating field, deep navy first title line, bright azure second title line, navy/azure structure, elevated glass/acrylic artifact, tactile 3D/isometric supply-chain desk, consulting-grade spacing. Use coral only for demand miss/changeover risk. Use eco-green only for verified/protected flow. Use amber only for WATCH states if needed. Avoid purple/orange cubes, beige plaque endings, generic colorful dashboard palettes, and old-fashioned serif headline.

## Composition

A large acrylic operating board dominates the middle-lower canvas. Use v1's richer feeling: tactile board screws, layered glass, detailed chart, named cause rows, distinct product-family tokens, clear connectors, detailed sequence lanes, small factory/material props. Use v2's cleaner background and navy/azure title. The board must be readable and valuable, not just pretty.

## Content

Heading: `DEMAND REVIEW TO SEQUENCE BOARD`

Subheading: `Turn forecast variance into planner-verified sequence options.`

Labels and micro-labels: `DEMAND MISS`; `ACTUAL > FCST`; `PULL-IN`; `LATE ORDER`; `ROOT CAUSE`; `PROMO LIFT`; `CUSTOMER PULLBACK`; `ORDER SHIFT`; `BASELINE HIGH`; `FAMILY IMPACT`; `FAMILY A`; `FAMILY B`; `FAMILY C`; `SERVICE RISK`; `CONSTRAINT CHECK`; `MATERIALS`; `CAPACITY`; `SERVICE`; `CHANGEOVER RISK`; `A TO B LOW`; `B TO C WATCH`; `C TO A HIGH`; `SEQUENCE A`; `SERVICE-FIRST`; `SEQUENCE B`; `LOW CHANGEOVER`; `SEQUENCE C`; `MATERIAL-SAFE`; `VERIFY`; `HUMAN SIGN-OFF`; `CLAUDE`.

Bottom question: `Which sequence decision changes first?`

## Logo Plan

Do not draw the actual Shetty's Desk logo or Claude mark. Create embedded blank logo/icon slots that will receive exact assets after generation. The slots must look designed into the image, not like empty boxes stuck on afterward.

## Integrity

This is an illustrative workflow example, not a sourced numeric model. No time savings, optimization scores, dollar amounts, or percentages. Make the demand miss visibly connect to family impact, then constraint checks, then sequence options.
