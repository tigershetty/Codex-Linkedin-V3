# Brand Story Exploration v14 — Demand Planning Corrections

## User Feedback Incorporated

Keep:

- elevated glass elements from both v13 renders,
- the connected element directly below the heading,
- render A heading style,
- render B decision miniatures / icon handling.

Fix:

- break the SC OS-like outer frame and desk-object perimeter,
- change hero placement so it does not feel like SC OS edited with a different center,
- remove the bottom question text box,
- add readable context to previously unlabeled map icons.

## Candidate A

**Prompt:** `gpt-image-2-prompt-v14a-evidence-rail-decision-table.md`  
**Generated file:** `gpt-image-2-candidate-v14a-evidence-rail-decision-table.png`  
**Artifact:** Demand Planning Decision Table with Evidence Rail  
**Intent:** Keep the stronger v13A heading and glass table language, but replace the SC OS-like surrounding props with a thin evidence rail and labeled demand-planning signals.

**Initial read:** strongest for business readability and exact decision context. It keeps the desired v13A heading style, removes the bottom question, and turns previously anonymous icons into labeled planning signals and decision actions. It still uses a rectangular board, so it is less differentiated than Candidate B, but the surrounding scene no longer feels like a direct SC OS object frame.

## Candidate B

**Prompt:** `gpt-image-2-prompt-v14b-decision-lens-map.md`  
**Generated file:** `gpt-image-2-candidate-v14b-decision-lens-map.png`  
**Artifact:** Demand Planning Decision Lens  
**Intent:** Push further away from the SC OS poster/table structure by making the central hero a lens/radar instrument over the forecast miss, with labeled evidence and decisions integrated around it.

**Initial read:** strongest for creative variety and breaks more clearly from SC OS. The hero is now a distinct demand-planning instrument rather than an edited poster/table, and the surrounding elements are context-labeled. The title changes the artifact from `Decision Table` to `Decision Lens`, which may be better visually but needs approval if we want the post title locked as Decision Book/Table.

## Logo Cleanup Note

Exact-logo cleanup variants were also generated:

- `gpt-image-2-candidate-v14a-evidence-rail-decision-table-logo-clean.png`
- `gpt-image-2-candidate-v14b-decision-lens-map-logo-clean.png`

These are not recommended. The overlay patch is too visible and interferes with the bottom learning loop. Use the raw v14 candidates for creative review. If either candidate is selected, do exact-logo cleanup as a separate design-layer pass with a cleaner reserved footer zone.

## Quality Bar

SC OS remains the finish benchmark only. It should not be used as the layout template. The successful result should feel like the same Shetty's Desk design studio created a different artifact for a different planning meeting.
