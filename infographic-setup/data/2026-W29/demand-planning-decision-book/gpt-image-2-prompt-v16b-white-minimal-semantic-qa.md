# GPT Image 2 Prompt v16B — White Minimal Decision Lens + Semantic QA

Use case: precise-object-edit / productivity-visual
Asset type: LinkedIn 3:4 portrait flagship visual
Edit target: `gpt-image-2-candidate-v14b-decision-lens-map.png`

Edit the v14B Demand Planning Decision Lens candidate. Use v14B as the base, not v15B.

## What To Preserve

- The minimal white / pale-blue Shetty's Desk background language from v14B and SC OS.
- The large two-tone title.
- The glass/acrylic decision lens hero.
- The elevated depth and dimensional decision miniatures.
- The circular context nodes around the lens.
- The green learning loop and bottom authorship.
- The clean luminous premium artifact feel.

## What To Correct

The background should remain minimal and white-ish, but less obviously AI-generated:

- keep the white/pale-blue negative space,
- remove the sci-fi corridor feeling and glowing wall panels,
- reduce perfectly symmetrical light beams,
- keep only a faint paper/grain texture and very subtle shadow falloff,
- use soft editorial lighting and tactile glass shadows,
- no realistic office clutter, no notebooks, no coffee, no plants, no busy printed boards,
- do not darken the image or make it beige/gray.

## Critical Semantic Correction

The current `FORECAST` and `ACTUAL` callouts are still too easy to misread. Fix this by using a clear legend/inset inside the lens instead of ambiguous arrows.

Create a small clean legend panel at the upper-left or lower-left edge of the lens:

- `FORECAST` with a blue line sample
- `ACTUAL` with a coral line sample
- `MISS` with a translucent coral gap sample
- `ROOT ASSUMPTION` with a bullseye/target dot sample

Then remove any arrows where `FORECAST` and `ACTUAL` appear to point to the same place.

Inside the chart:

- the blue curve must be visually distinct from the coral curve,
- `ROOT ASSUMPTION` must point only to the central bullseye/target marker,
- `MISS` must refer to the gap between the curves, not the red line itself.

## Exact Text Lock

Use only:

- DEMAND PLANNING
- DECISION LENS
- Find the assumption before changing the plan.
- ORDERS
- PROMO
- PRICE
- SERVICE
- MARKET
- FORECAST
- ACTUAL
- MISS
- ROOT ASSUMPTION
- MIX SHIFT
- LOST SALES
- PROMO LIFT
- LEAD TIME
- CHANNEL SHIFT
- INVENTORY
- Rebalance stock
- CAPACITY
- Shift load
- SUPPLY
- Expedite or defer
- CUSTOMER
- Commit or substitute
- LEARN
- Update assumptions
- Shetty's Desk

## Avoid

Avoid the v15 realistic office background. Avoid beige gray studio, notebooks, paper clutter, coffee cups, and wall charts. Avoid sterile sci-fi corridor, hologram walls, fake blue light beams, and over-glowing glass. Avoid losing depth in the decision miniatures. Avoid flattening icons into simple vector buttons. Avoid arrows that make `FORECAST`, `ACTUAL`, and `ROOT ASSUMPTION` point to the same object.
