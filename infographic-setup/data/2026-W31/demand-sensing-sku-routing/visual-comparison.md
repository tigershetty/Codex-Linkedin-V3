# Visual Comparison - Demand Sensing SKU Routing

**Week:** `2026-W31`
**Slug:** `demand-sensing-sku-routing`

## Candidates

| Candidate | Role | Verdict |
|---|---|---|
| `visual-v1-raw.png` | GPT Image 2 first render | Winning composition and text; retained as the untouched generation record. |
| `visual-v2-logo-locked.png` | first deterministic logo pass | Correct Claude chip; Shetty's Desk lockup sat too close to the lower placard rule. |
| `visual-v3-logo-larger.png` | enlarged deterministic logo pass | Exact Claude asset retained; Shetty's Desk lockup enlarged to 96 x 96 pixels around the same optical center. |
| `visual-v4-horizontal-logo.png` | horizontal deterministic logo pass | Replaces the square lockup with the official 282 x 74 horizontal asset so `DESK` remains legible inside the shallow nameplate. |
| `visual.png` | selected final | Pixel-identical to `visual-v4-horizontal-logo.png`; all non-logo visual details remain unchanged from the winning composition. |

## Why The Final Wins

The first generation already solved the difficult creative problem: a new visual architecture that reads as a physical planning policy rather than another dashboard. The mixed input pack, central five-test machine, two different primary cadence loops, and slim exception siding make the argument clear before the micro-labels are read.

The selected final preserves that composition without generative cleanup drift. It changes only the two reserved logo zones. The official Claude mark sits inside the dotted tool slot, while the exact horizontal Shetty's Desk lockup uses the placard's width so both `Shetty's` and `DESK` remain readable. No HTML control was produced because the user requested GPT Image 2 as the still-rendering lane for this sequence.

## Final Pick

`visual.png`
